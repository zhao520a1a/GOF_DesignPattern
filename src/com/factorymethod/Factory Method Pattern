工厂方法模式（Factory Method Pattern）：
	它是简单工厂方法模式的衍生; 它的核心工厂类不再负责产品的创建，核心类成为一个抽象工厂角色，仅负责具体工厂子类必须实现的接口，产品的创建则由它具体工厂子类实现；
  使得系统可以在不修改具体工厂角色的情况下引用新的产品；
   	一个抽象工厂对应一个抽象产品，一个具体工厂对应一个具体产品；
	它是最典型的模版方法模式（Template Method Pattern）应用；
	
工厂方法模式角色定义：
	（一个）抽象工厂角色：AbstractFactory
	具体工厂角色：ConcreteFactory
	（一个）抽象产品角色：AbstractProduct
	具体产品角色：ConcreteProduct